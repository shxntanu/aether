// Command aether starts the Aether HTTP API and manages its process lifecycle.
// It is the application's composition root: it loads configuration, constructs
// transport dependencies, and coordinates graceful startup and shutdown.
package main
