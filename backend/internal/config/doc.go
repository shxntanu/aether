// Package config loads and validates Aether's process configuration from the
// environment. It provides typed runtime settings while keeping environment
// access out of application and infrastructure packages.
package config
