// Package httpapi exposes Aether through HTTP. It owns route registration and
// HTTP request and response concerns while delegating business behavior to the
// application's domain services and ports.
package httpapi
